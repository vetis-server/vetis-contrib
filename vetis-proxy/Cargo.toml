[package]
name = "vetis-proxy"
version = "0.1.1"
edition.workspace = true
authors.workspace = true
repository.workspace = true
homepage.workspace = true
description = "Very Tiny Server Reverse Proxy support"
readme = "README.md"
license.workspace = true
keywords = ["http", "modules", "server", "rest-server"]
publish = true
rust-version.workspace = true

[features]
default = ["runtime-tokio"]
runtime-tokio = [
  "deboa-tokio/http1",
  "deboa-tokio/rust-tls",
  "deboa-tokio/default-rustls-provider",
  "deboa-tokio/default-rustls-verifier",
  "vetis-tokio/http1",
  "vetis-tokio/rust-tls",
]

[dependencies]
deboa = { version = "0.1.0" }
deboa-tokio = { version = "0.1.1", optional = true, default-features = false }
env_logger = "0.11.11"
http = "1.4.2"
http-body-util = "0.1.4"
once_cell = "1.21.4"
serde = { version = "1.0.228", features = ["derive"] }
vetis = { version = "0.1.4" }
vetis-tokio = { version = "0.1.0", optional = true, default-features = false }

[dev-dependencies]
caramelo = "0.1.2"
tokio = { version = "1.50.0", default-features = false, features = ["macros"] }
